#include "GeometryIndependent.h"
#include "Camera.h"

struct GeometryIndependent
{
	//TextureComponent
	float offsetX, offsetY;
};

void CreateGeometryIndependentCollision(/*StageEntity*/)
{
	//Find stage entity
	//Look at model using orthographic camera
	//Save to a texture with custom size (int8 per pixel)
	//Add this component to stage entity
	//Return.
}
